[project]
name = "pcap-playback"
version = "0.1.0"
description = "CLI tool to replay PCAP/PCAPNG network captures to a live interface"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "scapy>=2.6.0",
    "click>=8.1.0",
    "rich>=13.0.0",
]

[project.scripts]
pcap-playback = "pcap_playback.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pcap_playback"]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.4.0",
    "mypy>=1.10.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
