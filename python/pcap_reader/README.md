# pcap-reader

CLI tool to inspect and analyze PCAP/PCAPNG network capture files.

## Usage

```bash
uv run pcap-reader info capture.pcap
uv run pcap-reader list capture.pcap
uv run pcap-reader summary capture.pcap
```
